# Этот файл — НЕ ваши настройки. Ваши живут в config/lab.local.yaml.
#
# Он остался от версии 1.0.0, где настройки хранились здесь. Начиная с 1.0.1
# личный файл отделён: config/lab.local.yaml не попадает в репозиторий, и
# обновления его не трогают.
#
# Зачем файл всё ещё здесь: START_HERE.command при первом запуске смотрит,
# не остались ли у вас настройки в этом файле с прежней версии, и переносит
# их в личный. Если переносить нечего — берётся config/lab.yaml.example.
#
# ⚠️ Пустым он оставлен НАМЕРЕННО. До 01.09.2026 здесь лежала побайтная копия
#    lab.yaml.example — два одинаковых шаблона под разными именами. Из этой же
#    развилки вырос исходный дефект каркаса: документы показывали на один файл,
#    помощник читал другой, настройки владельца уходили в никуда.
#    Схема настроек — одна, и она в lab.yaml.example.
